# Frontend (Vite) environment variables
# Copy to .env and adjust per environment. Only keys prefixed with VITE_ are exposed to the browser.
VITE_API_BASE=https://phase45-backend-env.example.com
# Alternative name supported for backwards compatibility
# VITE_API_BASE_URL=https://phase45-backend-env.example.com

# Optional analytics or feature flags
# VITE_ENABLE_DEBUG=false
